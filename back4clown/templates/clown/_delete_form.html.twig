<form method="post" action="{{ path('app_clown_delete', {'id': clown.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ clown.id) }}">
    <button class="btn">Delete</button>
</form>
